// import { Action } from 'routing-controllers';
// import { Connection } from 'typeorm';

// import { Admin } from '../api/models/Admin';

// export function currentUserChecker(connection: Connection): (action: Action) => Promise<Admin | undefined> {
//     return async function innerCurrentUserChecker(action: Action): Promise<Admin | undefined> {
//         return action.request.user;
//     };
// }
